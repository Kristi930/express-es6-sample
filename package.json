{
  "name": "express-es6-sample",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=18.x" },
  "scripts": {
    "start": "node ./bin/www",
    "build": "echo \"No build step\"",
    "test": "node --version && npm -v && echo \"Running smoke test\" && node -e \"console.log('test ok')\""
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "debug": "^4.3.4",
    "express": "^4.19.2",
    "http-errors": "^2.0.0",
    "morgan": "^1.10.0",
    "pug": "^3.0.2"
  }
}

