version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm ci
  build:
    commands:
      - echo Building app...
      - npm run build || echo "No build script"
artifacts:
  files:
    - '**/*'

